(admin.html and adnd2_character_sheet.html are HTML source files, which are not build artifacts, dependencies, or temporary files - they are legitimate source code files that should be tracked in version control)

(empty - output nothing)